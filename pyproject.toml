[project]
name = "password-strength-analyzer"
version = "1.0.0"
description = "A command-line tool for analyzing password strength and patterns."
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "almotery" }
]
license = { text = "MIT" }
